# 开发整改事项清单（PM 强制执行）

> 适用范围：`AI-music-producer` 全体开发成员
> 执行标准：`PM_RULES + PRD + one law` 三基线一致
> 口径：证据优先、真实数据、真实链路、可复现

---

## A. 最高优先级（P0，必须先完成）

1. 修复主链路命令可用性
- 目标：`apps/cli/main.py` 与 `src/main.py` 的命令链路稳定可执行，不再出现参数解析崩溃。
- 验收：真实场景命令可执行并生成三件套与追溯文件。
- 证据：命令、返回码、产物路径。

2. 打通模型提供方适配层（Anthropic / OpenAI-compatible）
- 目标：在不违反 PRD 约束前提下，主链路支持当前环境可用模型通道。
- 验收：`.env` 中可用 key 能驱动同一主链路完成生成。
- 证据：`trace.json` 包含 llm_calls 与调用链路字段。

3. 落地 PRD v2.1 Step1：retriever + corpus 注入
- 目标：实现 `src/retriever.py`，按 emotion_tags + valence 检索 2-3 条示例注入。
- 验收：`lyric_payload.json` 出现 `few_shot_examples_used` 且数量 >= 2。
- 证据：示例 source_id、title、matched tags。

4. 落地 PRD v2.1 Step2/3：三变体生成与排序选优
- 目标：输出 3 个 variants，lint 对三变体评分排序并给出 `chosen_variant_id`。
- 验收：`variants` 恰好 3 个，POV 各不相同，存在 rank。
- 证据：`lyric_payload.json` + `trace.json` + lint 报告。

5. 落地 PRD v2.1 Step4：真实 Targeted Revise
- 目标：Top-1 违规时触发一次真实定向重写，不允许仅写 prompt 到 trace。
- 验收：触发场景下 llm_calls 从 1 变 2，违规项减少或清零。
- 证据：before/after lint 对比、trace 中 revise 记录。

---

## B. 高优先级（P1，本周闭环）

6. 强化 G4 失败可观测性
- 目标：`docs-alignment-check` fail 时打印 `failed_checks`。
- 验收：缺失 `manifest_path` 时 CLI 显式输出 `failed_checks: manifest_path`。

7. 增加真实 E2E 回归用例
- 目标：新增 1 条“真实路径 + 真实意图 + 真实产物”回归流程。
- 验收：固定命令可复跑并产出 `out/*` 五件套。

8. 完善 G3 输出一致性判定
- 目标：避免关键词误判（如“not pass”被当 pass）。
- 验收：增加反例测试并通过。

9. 统一审计模板自动化
- 目标：每个 Gate PR 自动生成四段审计骨架与命令输出归档。
- 验收：新 PR 默认带模板，不再手工拼接。

---

## C. 工程质量（P2，持续改进）

10. 建立“禁止旁路生成”守卫
- 目标：CI 或 hooks 检测旁路脚本调用模型行为（规则白名单）。
- 验收：发现旁路调用时阻断并给出定位信息。

11. 建立“语义可解释性”人工抽检标准
- 目标：针对歌词可理解性、意象一致性、句间因果建立人工审稿打分表。
- 验收：每次版本至少 3 首样本盲评记录。

12. 版本化语料质量基线
- 目标：corpus 每次更新均附统计摘要（标签覆盖、长度分布、重复率）。
- 验收：语料 PR 必带统计与回滚点。

---

## 交付要求（逐条关闭）

每条整改项必须在 PR 描述中包含：

1. 变更文件路径
2. 对应测试用例
3. 执行命令与结果
4. 回滚方案

未满足上述 4 项，视为未完成。

---

## D. 今日审计定位（2026-04-21，按文件整改）

以下问题为本日真实审计结论，开发团队必须按“文件定位 -> 修改动作 -> 验收证据”逐条关闭。

1. **缺少 PRD Step1 检索模块（阻断项）**
- 现状：仓库不存在 `src/retriever.py`，动态 few-shot 未落地。
- 修改位置：`src/retriever.py`（新增）、`src/main.py`（接入）、`src/claude_client.py`（注入 few-shot）。
- 必做动作：实现 emotion_tags + valence 检索，返回 2-3 条示例（至少 1 古典 + 1 现代）。
- 验收证据：`lyric_payload.json` 出现 `few_shot_examples_used` 且 `>=2`。

2. **生成结构仍为单稿，未实现三变体（阻断项）**
- 现状：`src/schemas.py` 的 `LyricPayload` 仍是单份 `lyrics_by_section`，缺少 `variants/chosen_variant_id`。
- 修改位置：`src/schemas.py`、`src/claude_client.py`、`src/compile.py`。
- 必做动作：按 PRD 改为三变体结构并保留选优结果字段。
- 验收证据：`variants` 恰好 3 个，POV 各不相同，存在 `chosen_variant_id`。

3. **lint 未按三变体排序选优（阻断项）**
- 现状：`src/lint.py` 仅对单 payload 校验，未执行 variants 排序与 Top-1 选择。
- 修改位置：`src/lint.py`、`src/main.py`。
- 必做动作：对 3 variants 分别跑规则，输出 rank，按 PRD 规则排序选优。
- 验收证据：`lint_report` 包含各 variant 评分、失败规则、rank 与 Top-1。

4. **Targeted Revise 未真实执行（阻断项）**
- 现状：`src/main.py` 仅记录 revise prompt 到 trace，未执行第 2 次模型调用。
- 修改位置：`src/main.py`、`src/claude_client.py`。
- 必做动作：Top-1 违规时触发真实定向重写并回填最终 payload。
- 验收证据：触发样例中 `llm_calls` 从 1 变 2，且违规数下降。

5. **模型通道与环境不一致（阻断项）**
- 现状：`src/claude_client.py` 仅支持 `ANTHROPIC_API_KEY`，与当前 `.env` 中主用 GPT/Kimi 通道不一致。
- 修改位置：`src/claude_client.py`、`README.md`、`docs/🎵 AI 音乐生成系统产品经理 (PM) Role & Rule.md`（若策略变更需同步）。
- 必做动作：实现主链路模型适配层（Anthropic 与 OpenAI-compatible），禁止旁路临时调用。
- 验收证据：同一 `produce` 主链路在可用 key 下成功生成并保留 trace。

6. **CLI 端到端命令稳定性不足（高优先级）**
- 现状：`python -m apps.cli.main produce ...` 存在参数解析异常（Typer/参数链路）。
- 修改位置：`apps/cli/main.py`、相关 CLI 测试文件。
- 必做动作：修复命令解析并补充 return code + 参数覆盖测试。
- 验收证据：真实场景命令可稳定执行，返回码为 0。

7. **真实数据 E2E 缺失审计模板（高优先级）**
- 现状：已有 gate 证据，但缺“真实路径 + 真实输入 + 真实产物 + FAIL 对照”的固定模板。
- 修改位置：`docs/🎵 AI 音乐生成系统产品经理 (PM) Role & Rule.md`、PR 模板文件（如后续新增）。
- 必做动作：新增固定审计模板并强制每次发版附带。
- 验收证据：PR 正文含真实路径样本、PASS/FAIL 快照、产物文件路径。

8. **旁路产物治理（已执行一次，后续持续）**
- 现状：已清理一次旁路生成文件；后续需防再引入。
- 修改位置：`tools/githooks/pre-commit` 或 `tools/scripts/run_quality_gates_ci.sh`。
- 必做动作：增加旁路产物扫描规则（例如 `out/e2e_*` 非白名单阻断）。
- 验收证据：提交旁路产物时 pre-commit/CI 直接 fail。
